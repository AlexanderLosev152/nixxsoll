import classNames from 'classnames';

import style from './style.module.scss';

export const Testimonials = (props: Props) => {
	return (
		<section className={classNames(style.testimonials, 'section', 'container')}>
			Testimonials
		</section>
	);
};
